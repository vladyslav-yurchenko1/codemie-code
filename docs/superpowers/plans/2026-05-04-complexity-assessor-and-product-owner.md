# Complexity Assessor Agent + Product Owner Skill — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the inline complexity-assessment skill with an isolated subagent, and add a product-owner skill that generates functional requirements from any user input.

**Architecture:** The complexity-assessor becomes a named agent (created via agent-creator) that tech-lead dispatches by name — the agent does its own codebase research and returns only the assessment block, keeping tech-lead's context clean. The product-owner skill is a new user-facing skill that orchestrates an Explore subagent and superpowers:brainstorming (non-technical scope) to produce a functional requirements doc with story breakdown and optional ticket creation.

**Tech Stack:** Markdown skill files, Claude Code agent definitions, existing superpowers plugin system.

---

## File Map

| Action | Path | Purpose |
|--------|------|---------|
| DELETE | `.claude/skills/complexity-assessment/SKILL.md` | No longer a user-invokable skill |
| CREATE | `.claude/agents/complexity-assessor.md` | Named agent definition — generated via agent-creator |
| MODIFY | `.claude/skills/tech-lead/SKILL.md` | Step 5 dispatch, routing thresholds, integration notes |
| CREATE | `.claude/skills/product-owner/SKILL.md` | New PO skill |

Reference files that stay untouched:
- `.claude/skills/complexity-assessment/references/complexity-assessment-guide.md`
- `.claude/skills/complexity-assessment/references/complexity-examples-*.md`

---

## Group A: Complexity Assessor Agent

---

### Task A1: Remove complexity-assessment SKILL.md

**Files:**
- Delete: `.claude/skills/complexity-assessment/SKILL.md`

- [ ] **Step 1: Confirm the file exists before deleting**

Run:
```bash
ls .claude/skills/complexity-assessment/
```
Expected output: `SKILL.md  references/`

- [ ] **Step 2: Delete the SKILL.md**

```bash
git rm -f .claude/skills/complexity-assessment/SKILL.md
```

Expected: `rm '.claude/skills/complexity-assessment/SKILL.md'`

- [ ] **Step 3: Verify reference files are untouched**

Run:
```bash
ls .claude/skills/complexity-assessment/references/
```
Expected: all `complexity-assessment-guide.md` and `complexity-examples-*.md` files still present.

- [ ] **Step 4: Commit**

```bash
git commit -m "feat(skills): remove complexity-assessment skill — replaced by complexity-assessor agent"
```

---

### Task A2: Create complexity-assessor agent via agent-creator

**Files:**
- Create: `.claude/agents/complexity-assessor.md` (generated by agent-creator)

- [ ] **Step 1: Create the `.claude/agents/` directory**

```bash
mkdir -p .claude/agents
```

- [ ] **Step 2: Invoke the agent-creator**

Use the Agent tool with `subagent_type="plugin-dev:agent-creator"` and provide these exact requirements:

```
Create an agent named "complexity-assessor".

Purpose: Perform independent codebase complexity assessment as a subagent dispatched
by tech-lead. Runs in isolation so it does not pollute tech-lead's context.

Caller: tech-lead passes two inputs:
  - task_description: what needs to be built (from Phase 1 requirements)
  - feature_area: keywords describing the domain (e.g. "provider integration", "CLI command")

Behavior (in order):
1. Read `.claude/skills/complexity-assessment/references/complexity-assessment-guide.md`
   — load all 6 dimensions, the complexity matrix, red flags, and best practices.
2. Glob `.claude/skills/complexity-assessment/references/complexity-examples-*.md`
   and read each file as calibration anchors.
3. Use Glob and Grep to research the codebase based on task_description and feature_area:
   — find files likely affected by the change
   — identify which architectural layers are touched (UI, API, Service, DB, Infra, External)
   — count approximate file change scope
4. Score each of the 6 dimensions (XS=1, S=2, M=3, L=4, XL=5, XXL=6) per the guide.
5. Apply red flags from the guide — bump the affected dimension up by 1 tier where applicable.
6. Return the structured assessment block below.

Strict output rules:
- No code snippets anywhere in the output
- Reference component names, file paths, and layer labels only — no implementation details
- Do not reproduce guide content — scoring output only
- Total output must be ≤300 words

Required output format (return exactly this structure):

## Complexity Assessment: [feature area]

### Dimension Scores:
| Dimension            | Score | Label   |
|----------------------|-------|---------|
| Component Scope      | [1-6] | [XS–XXL] |
| Requirements Clarity | [1-6] | [XS–XXL] |
| Technical Risk       | [1-6] | [XS–XXL] |
| File Change Estimate | [1-6] | [XS–XXL] |
| Dependencies         | [1-6] | [XS–XXL] |
| Affected Layers      | [1-6] | [XS–XXL] |

### Total: [sum]/36 — [XS | S | M | L | XL | XXL]

### Key Reasoning:
- **[Highest dimension]**: [why — component names, not code]
- **[Second highest]**: [why]
- **[Red flags applied]**: [which dimension bumped and why, or "none"]

### Routing:
[superpowers:writing-plans | superpowers:brainstorming | SPLIT REQUIRED]

Routing rules:
- Score 6–9:   XS → superpowers:writing-plans
- Score 10–14: S  → superpowers:writing-plans
- Score 15–20: M  → superpowers:brainstorming
- Score 21–26: L  → superpowers:brainstorming
- Score 27–31: XL → SPLIT REQUIRED — present splitting strategies, wait for user decomposition
- Score 32–36: XXL → SPLIT REQUIRED — hard block, do not invoke any planning skill

Save the agent definition to: .claude/agents/complexity-assessor.md
```

- [ ] **Step 3: Verify the file was created at the correct path**

```bash
ls .claude/agents/
```
Expected: `complexity-assessor.md`

If the agent-creator saved it elsewhere, move it:
```bash
mv <wherever-it-was-saved> .claude/agents/complexity-assessor.md
```

- [ ] **Step 4: Read and spot-check the generated file**

Read `.claude/agents/complexity-assessor.md` and confirm:
- Contains the 6-dimension scoring table
- Contains the routing rules (6–9 XS through 32–36 XXL)
- Output rules section present (no code snippets, ≤300 words)
- Routing block format matches the required template

- [ ] **Step 5: Force-add and commit** (`.claude/agents/` is gitignored like `.claude/skills/`)

```bash
git add -f .claude/agents/complexity-assessor.md
git commit -m "feat(agents): add complexity-assessor agent — 6-dimension scoring, context-isolated"
```

---

### Task A3: Update tech-lead to dispatch complexity-assessor agent

**Files:**
- Modify: `.claude/skills/tech-lead/SKILL.md`

Six targeted edits. Apply them in order.

---

**Edit 1 — description frontmatter: "5 dimensions" → "6 dimensions"**

- [ ] **Step 1: Apply edit**

Find (line 3, in the `description:` value):
```
complexity scoring → brainstorming (complex) or writing-plans (simple) → spec-reviewer → subagent-driven-development → qa-lead. Also triggers on:
```

This is the description field — no change needed there. The dimension count is in the Purpose section.

Find in the Purpose section (line 14):
```
- Scores complexity across 5 dimensions to route to the right planning path
```
Replace with:
```
- Scores complexity across 6 dimensions to route to the right planning path
```

---

**Edit 2 — SDLC flow diagram: update score thresholds**

- [ ] **Step 2: Apply edit**

Find:
```
Score 5–7  → superpowers:writing-plans → spec-reviewer → superpowers:subagent-driven-development
Score 8–15 → superpowers:brainstorming → superpowers:writing-plans → spec-reviewer → superpowers:subagent-driven-development
```
Replace with:
```
Score 6–14  → superpowers:writing-plans → spec-reviewer → superpowers:subagent-driven-development
Score 15–36 → superpowers:brainstorming → superpowers:writing-plans → spec-reviewer → superpowers:subagent-driven-development
```

---

**Edit 3 — Step 5: replace skill invocation with agent dispatch**

- [ ] **Step 3: Apply edit**

Find:
```
### Step 5: Complexity Scoring

Invoke the `complexity-assessment` skill, passing:
- Task description (from Phase 1)
- Guide findings (from Step 4 above)
- Affected files list (from codebase search in Step 4)

```
Invoke Skill: complexity-assessment
Provide: task description, guide findings, affected files list
```

`complexity-assessment` will return a score breakdown (5–15) and a routing decision. Use that routing decision to proceed.
```
Replace with:
```
### Step 5: Complexity Scoring

Dispatch the `complexity-assessor` agent, passing:
- Task description (from Phase 1)
- Feature area / keywords (e.g. "provider integration", "CLI command")

```
Dispatch Agent: complexity-assessor
Provide: task description, feature area
```

`complexity-assessor` will return a score breakdown (6–36) and a routing decision. Use that routing decision to proceed.
```

---

**Edit 4 — Phase 3 routing thresholds**

- [ ] **Step 4: Apply edit**

Find:
```
### Simple Path (Score 5–7)
```
Replace with:
```
### Simple Path (Score 6–14)
```

Find:
```
### Medium/Complex Path (Score 8–15)
```
Replace with:
```
### Medium/Complex Path (Score 15–36)
```

Find (in Medium/Complex Path body):
```
Invoke `superpowers:brainstorming`, passing requirements summary, guide findings, complexity score, and open architectural questions.
```
Replace with:
```
Invoke `superpowers:brainstorming`, passing requirements summary, guide findings, complexity score (6–36), and open architectural questions.
```

---

**Edit 5 — Integration section: replace complexity-assessment entry**

- [ ] **Step 5: Apply edit**

Find:
```
### complexity-assessment
- Invoked at the end of Phase 2 (Step 5) with task description, guide findings, and affected files
- Scores complexity across 5 dimensions (5–15 total) and returns routing decision
- Self-contained: owns its own scoring guide and calibration examples
```
Replace with:
```
### complexity-assessor (agent)
- Dispatched at the end of Phase 2 (Step 5) with task description and feature area
- Runs in isolated context — performs its own codebase research via Glob/Grep
- Scores complexity across 6 dimensions (6–36 total) and returns routing decision
- Guide and calibration examples live in `.claude/skills/complexity-assessment/references/`

### superpowers:brainstorming (score 15–36 path)
```

Wait — there is already a `### superpowers:brainstorming` entry below. Do not add a duplicate heading. The replacement is just the complexity-assessment block:

Find:
```
### complexity-assessment
- Invoked at the end of Phase 2 (Step 5) with task description, guide findings, and affected files
- Scores complexity across 5 dimensions (5–15 total) and returns routing decision
- Self-contained: owns its own scoring guide and calibration examples
```
Replace with:
```
### complexity-assessor (agent)
- Dispatched at the end of Phase 2 (Step 5) with task description and feature area
- Runs in isolated context — performs its own codebase research via Glob/Grep
- Scores complexity across 6 dimensions (6–36 total) and returns routing decision
- Guide and calibration examples live in `.claude/skills/complexity-assessment/references/`
```

---

**Edit 6 — brainstorming integration note: update score range**

- [ ] **Step 6: Apply edit**

Find:
```
### superpowers:brainstorming
- Invoked for Medium/Complex features (score 8–15)
```
Replace with:
```
### superpowers:brainstorming
- Invoked for Medium/Complex features (score 15–36)
```

Find:
```
### superpowers:writing-plans
- Invoked for Simple features (score 5–7) directly, or after brainstorming
```
Replace with:
```
### superpowers:writing-plans
- Invoked for Simple features (score 6–14) directly, or after brainstorming
```

---

- [ ] **Step 7: Read the full modified tech-lead SKILL.md and verify**

Check:
- No remaining references to "5 dimensions", "5–15", "Score 5–7", "Score 8–15", or "complexity-assessment skill"
- Step 5 reads "Dispatch Agent: complexity-assessor"
- Routing thresholds are 6–14 (simple) and 15–36 (medium/complex)
- Integration section shows `complexity-assessor (agent)` not `complexity-assessment`

- [ ] **Step 8: Commit**

```bash
git add -f .claude/skills/tech-lead/SKILL.md
git commit -m "feat(skills): update tech-lead to dispatch complexity-assessor agent — 6-dimension scoring"
```

---

## Group B: Product Owner Skill

---

### Task B1: Create product-owner skill

**Files:**
- Create: `.claude/skills/product-owner/SKILL.md`

- [ ] **Step 1: Create the directory**

```bash
mkdir -p .claude/skills/product-owner
```

- [ ] **Step 2: Write the skill file**

Create `.claude/skills/product-owner/SKILL.md` with this exact content:

```markdown
---
name: product-owner
description: Use when a user wants to turn an idea, wireframe, screenshot, or plain-text description into a detailed functional requirements document with story breakdown. Triggers on: "act as product owner", "create requirements for", "write functional requirements", "I have an idea help me spec it out", "create stories for", "break this into user stories", "write user stories", "define acceptance criteria", "create a PRD", "create an FRD". This skill is non-technical — it produces business requirements, not architecture docs. Always invoke it when the user describes a feature idea without asking for code or implementation.
version: 0.1.0
---

# Product Owner: Functional Requirements Generator

## Purpose

Transform raw user input — text descriptions, wireframes, screenshots, mockups — into a structured functional requirements document (FRD) with epic and story breakdown. Output is non-technical: business language, user flows, acceptance criteria. No code, no architecture.

## Input Formats Accepted

- Plain text description of an idea or feature
- Wireframe or mockup images
- Screenshots of existing flows or competitor products
- Links to existing tickets or docs
- Any combination of the above

If the user provides a one-liner only, ask for more context before proceeding. Never infer requirements from minimal input.

## Flow

```
Step 1: Input Collection
Step 2: Clarifying Questions (one at a time — max 5 questions)
Step 3: Explore Subagent — map existing capabilities
Step 4: superpowers:brainstorming — non-technical scope, visual companion
Step 5: FRD Assembly → save to docs/product/
Step 6: Story Breakdown → append to FRD
Step 7: Ticket Creation (optional, always confirm first)
```

---

## Step 1: Input Collection

Acknowledge what the user provided. If images/wireframes were shared, describe what you see to confirm understanding before asking questions.

---

## Step 2: Clarifying Questions

Ask one question at a time until all five answers are clear:

1. **Who are the target users?** Describe them as people, not roles — what do they do, what frustrates them today?
2. **What problem does this solve?** The job-to-be-done in one sentence.
3. **What does success look like?** A measurable outcome or observable user behavior change.
4. **What is explicitly out of scope for this release?**
5. **Are there constraints?** Brand, accessibility, regulatory, device, language, or deadline constraints.

Do not ask technical questions (stack, database, API design, etc.). If the user volunteers technical details, note them but do not let them drive the requirements.

---

## Step 3: Explore Subagent

Dispatch `Agent(subagent_type="Explore")` with this prompt (fill in `[feature area]` from context):

```
Research the existing system for capabilities related to [feature area].

Find:
1. Existing flows, screens, or features that overlap with or relate to [feature area]
2. User-facing capabilities that already exist
3. Gaps between current capabilities and what is being requested

Return a capability map:
- What already exists (feature names, screen names, flow names — no code)
- What is missing or partially supported
- What overlaps with the new request

Keep findings at the concept level. No code snippets. Max 200 words.
```

Use the capability map to ground the FRD in current system reality. Note overlaps and gaps explicitly in the FRD.

---

## Step 4: superpowers:brainstorming (non-technical scope)

Invoke `superpowers:brainstorming` with this framing passed as context:

**In scope:**
- User flows and journey maps
- Screen states, transitions, and empty states
- Business rules and decision points
- Edge cases from the user's perspective
- Visual structure — offer the visual companion early for wireframes, flow diagrams, and mockup comparisons
- Acceptance scenarios ("given X, when Y, then Z")

**Out of scope — redirect if raised:**
- Architecture, tech stack, database schema
- API design or system internals
- Code patterns or performance details

The visual companion should be offered at the start of brainstorming — wireframes and flow diagrams are primary deliverables of this step.

Brainstorming output is the primary input to Step 5.

---

## Step 5: FRD Assembly

Save to `docs/product/YYYY-MM-DD-[feature-name]-requirements.md`.

Use this exact structure:

```markdown
# [Feature Name] — Functional Requirements

**Date:** YYYY-MM-DD
**Status:** Draft
**Author:** Product Owner (AI-assisted)

---

## Business Context

[Problem statement: what pain exists today, who experiences it, why it matters now]

---

## Target Users

### [Persona Name]
[Who they are in plain language. What they need. What frustrates them today.]

### [Persona Name 2] (if applicable)

---

## Goals & Success Criteria

- [ ] [Measurable outcome — observable user behavior or business metric]
- [ ] [Measurable outcome 2]

---

## Functional Requirements

### FR-001: [Requirement name]
[What the system must do — one clear statement in plain language]
**Priority:** Must Have | Should Have | Nice to Have

### FR-002: [Requirement name]
...

---

## User Flows

### Flow: [Flow name]
[Numbered steps describing the user journey. No technical steps. Written as "User does X, system shows Y."]

1. User [action]
2. System [response]
3. User [action]
4. System [response]
...

---

## Visual References

[Wireframes, mockups, or flow diagrams produced in Step 4. Embed or link.]

---

## Out of Scope

- [What is explicitly excluded from this release]

---

## Open Questions

- [Unresolved items that need stakeholder input before implementation]

---
```

---

## Step 6: Story Breakdown

Append directly to the requirements doc after the `---` at the end:

```markdown
## Story Breakdown

### Epic: [Epic name]
[One sentence: the user outcome this epic delivers]

#### Story [E].[N]: [Story title]
**As a** [persona], **I want** [goal] **so that** [outcome].

**Acceptance Criteria:**
- [ ] Given [context], when [action], then [result]
- [ ] Given [context], when [action], then [result]

**Size:** [XS | S | M | L] ← business complexity, not technical effort
**Priority:** Must Have | Should Have | Nice to Have

---
```

Rules for stories:
- Each story delivers independently observable value to the user
- No story depends on another story being done first (within the same release)
- Every story has at least two acceptance criteria
- Tasks (sub-steps within a story) are only added when a story has distinct sequential steps the team would need to track separately

---

## Step 7: Ticket Creation (optional)

After saving and committing the doc, check for available integrations:

**Jira** — if the `brianna` skill is available:
```
Ask user: "I can create these epics and stories in Jira. Each story becomes one Jira issue with acceptance criteria in the description. Shall I proceed?"
```
If confirmed: use `brianna` to create one issue per story. Epic becomes the Jira Epic link. Acceptance criteria go in the description field.

**Other trackers** — if Azure DevOps, Linear, or other integrations are configured and available, use them the same way.

**No integration available** — output a copy-paste export block:

```markdown
## Export: Stories for Ticket Tracker

### Epic: [Epic name]
| Story | Priority | Acceptance Criteria (summary) |
|-------|----------|-------------------------------|
| [Story title] | [P] | [AC 1]; [AC 2] |
| [Story title] | [P] | [AC 1]; [AC 2] |
```

**Always ask before creating tickets. Never create automatically.**

---

## Key Principles

**Do's:**
- Use plain, non-technical language at all times
- Anchor every requirement to a user goal or business outcome
- Offer the visual companion early — wireframes are primary outputs, not illustrations
- Keep stories independently deliverable
- Ask before creating tickets
- Note existing capabilities (from Step 3) so the team knows what already works

**Don'ts:**
- No code snippets anywhere in the FRD or story breakdown
- No architecture, tech stack, or implementation decisions
- No assumptions about database, API, or infrastructure
- No story without at least two acceptance criteria
- Never auto-create tickets
```

- [ ] **Step 3: Verify the file was written correctly**

Read `.claude/skills/product-owner/SKILL.md` and confirm:
- Frontmatter has `name: product-owner`, `version: 0.1.0`, and a description with trigger phrases
- All 7 steps present (Input Collection → Ticket Creation)
- FRD template present with correct markdown structure
- Story breakdown template present
- No code snippets in the output templates
- Key Principles section present

- [ ] **Step 4: Force-add and commit**

```bash
git add -f .claude/skills/product-owner/SKILL.md
git commit -m "feat(skills): add product-owner skill — FRD generator with story breakdown and ticket creation"
```

---

## Final Step: Push and Verify

- [ ] **Push the branch**

```bash
git push
```

- [ ] **Verify all changes on the branch**

```bash
git log --oneline -5
```

Expected commits (most recent first):
```
feat(skills): add product-owner skill — FRD generator with story breakdown and ticket creation
feat(skills): update tech-lead to dispatch complexity-assessor agent — 6-dimension scoring
feat(agents): add complexity-assessor agent — 6-dimension scoring, context-isolated
feat(skills): remove complexity-assessment skill — replaced by complexity-assessor agent
```

- [ ] **Spot-check: confirm no broken references**

Run:
```bash
grep -r "complexity-assessment" .claude/skills/ --include="*.md" -l
```
Expected: no output (all references now point to `complexity-assessor`).

```bash
grep -r "Score 5" .claude/skills/tech-lead/SKILL.md
grep -r "Score 8" .claude/skills/tech-lead/SKILL.md
```
Expected: no output (old thresholds fully replaced).
